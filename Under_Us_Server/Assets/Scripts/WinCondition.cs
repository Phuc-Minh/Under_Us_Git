using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WinCondition : MonoBehaviour
{
    public static void VerifyWinCondition(int idWin)
    {
        // When all tasks is finished 
        if(idWin == 1)
        {

            return;
        }

        // Check number of player in each role
        if (idWin == 2)
        {

            return;
        }
    }
}
